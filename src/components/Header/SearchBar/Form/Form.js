import React from 'react';

const Form = () => {
    return (
        <div>
            <form action="">
                <input type="text"/>
                <button>Search</button>
            </form>
        </div>
    );
};

export default Form;
